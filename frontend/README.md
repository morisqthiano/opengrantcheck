# OpenGrantCheck Frontend

React + Vite frontend for the OpenGrantCheck V0.1 prototype.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

The default API base URL is configured in `.env.example`:

```text
VITE_API_BASE_URL=http://localhost:8000/api
```
